/// <reference types="vite/client" />

// Optional: declare your Vite environment variables here so TypeScript knows about them.
// Add VITE_ prefixed variables as needed, for example:
// declare global {
//   interface ImportMetaEnv {
//     readonly VITE_API_URL?: string
//   }
// }

// The reference above provides the base types (including ImportMeta.env).
// Export nothing to keep this file as a global declaration file.
export {}
